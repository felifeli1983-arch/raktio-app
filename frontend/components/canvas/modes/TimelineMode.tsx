"use client";
/** Raktio Canvas — TimelineMode (placeholder) */
export function TimelineMode() {
  return <div className="h-full flex items-center justify-center text-muted-foreground">TimelineMode — TODO</div>;
}
